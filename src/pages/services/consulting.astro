---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE } from '../../consts';
---

<!doctype html>
<html lang="zh-Hant">
	<head>
		<BaseHead title={`代謝財富教練系統 - ${SITE_TITLE}`} description="協助你掌握代謝靈活性，逆轉三高與慢性疲勞。" />
		<style>
            .lp-hero {
                background: linear-gradient(180deg, var(--bg-mint) 0%, var(--bg-cream) 100%);
                color: var(--black);
                padding: 5rem 1.5rem 6rem;
                text-align: center;
            }
            .lp-hero h1 {
                color: rgb(var(--black));
                margin-bottom: 1.5rem;
                font-size: 2.25rem;
                font-weight: 700;
                line-height: 1.3;
            }
            @media (min-width: 768px) {
                .lp-hero h1 { font-size: 2.75rem; }
            }
            .lp-hero p {
                color: rgb(var(--gray));
                font-size: 1.15rem;
                max-width: 700px;
                margin: 0 auto 2.5rem;
                line-height: 1.7;
            }

            .cta-btn {
                display: inline-flex;
                align-items: center;
                padding: 1rem 2rem;
                background: var(--accent-gradient);
                color: white;
                font-weight: 600;
                text-decoration: none;
                transition: all var(--transition-fast);
                border: none;
                border-radius: var(--border-radius);
                box-shadow: 0 4px 14px rgba(5, 150, 105, 0.35);
                font-size: 0.95rem;
            }
            .cta-btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 6px 20px rgba(5, 150, 105, 0.4);
            }
            .cta-btn.secondary {
                background: white;
                color: var(--accent);
                border: 1.5px solid rgba(5, 150, 105, 0.2);
                box-shadow: var(--box-shadow);
            }
            .cta-btn.secondary:hover {
                background: var(--bg-mint);
                border-color: var(--accent);
            }
            .cta-group {
                display: flex;
                flex-direction: column;
                gap: 1rem;
                align-items: center;
            }
            @media (min-width: 480px) {
                .cta-group {
                    flex-direction: row;
                    justify-content: center;
                    gap: 1.25rem;
                }
            }

            .section {
                padding: 5rem 1.5rem;
                max-width: 960px;
                margin: 0 auto;
            }

            .feature-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 1.75rem;
                margin-top: 3rem;
            }
            .feature-card {
                padding: 2rem;
                background: white;
                box-shadow: var(--box-shadow);
                border-radius: var(--border-radius-lg);
                transition: all var(--transition-medium);
                border: 1px solid rgba(0, 0, 0, 0.04);
                position: relative;
                overflow: hidden;
            }
            .feature-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 4px;
                background: var(--accent-gradient);
                opacity: 0;
                transition: opacity var(--transition-fast);
            }
            .feature-card:hover {
                transform: translateY(-6px);
                box-shadow: var(--box-shadow-lg);
            }
            .feature-card:hover::before {
                opacity: 1;
            }
            .feature-card h3 {
                color: rgb(var(--black));
                margin-top: 0;
                font-size: 1.15rem;
                font-weight: 600;
                margin-bottom: 0.75rem;
            }
            .feature-card p {
                color: rgb(var(--gray));
                font-size: 0.95rem;
                line-height: 1.7;
                margin: 0;
            }

            .workflow-step {
                display: flex;
                gap: 1.5rem;
                margin-bottom: 2.5rem;
                align-items: flex-start;
            }
            .step-number {
                background: var(--accent-gradient);
                color: white;
                width: 44px;
                height: 44px;
                display: flex;
                align-items: center;
                justify-content: center;
                font-weight: 600;
                flex-shrink: 0;
                border-radius: var(--border-radius);
                font-size: 1.1rem;
                box-shadow: 0 4px 10px rgba(5, 150, 105, 0.25);
            }
            .workflow-step h3 {
                font-size: 1.1rem;
                color: rgb(var(--black));
                margin-bottom: 0.5rem;
                font-weight: 600;
            }
            .workflow-step p {
                color: rgb(var(--gray));
                font-size: 0.95rem;
                line-height: 1.7;
                margin: 0;
            }
            .section-title {
                text-align: center;
                font-size: 1.75rem;
                margin-bottom: 1rem;
                color: rgb(var(--black));
                font-weight: 700;
            }
		</style>
	</head>
	<body>
		<Header />
		<main style="padding: 0; width: 100%; max-width: 100%;">
            <section class="lp-hero">
                <h1>不要讓你的成就，<br/>被失控的代謝拖垮</h1>
                <p>三高、脂肪肝、慢性疲勞...這些不是老化的正常現象，而是身體發出的求救訊號。
                我將用生物醫學的科學方法，協助你找回身體的「代謝主控權」。</p>
                <a href="/booking" class="cta-btn">預約免費代謝健康評估</a>
            </section>

            <section class="section">
                <h2 class="section-title">代謝財富教練系統</h2>
                <div class="feature-grid">
                    <div class="feature-card">
                        <h3>🧪 科學化數據解讀</h3>
                        <p>不再盲目嘗試偏方。我們從健檢報告與 CGM (連續血糖監測) 數據出發，精準找出你的代謝破口。</p>
                    </div>
                    <div class="feature-card">
                        <h3>🥑 低碳與斷食策略</h3>
                        <p>建立適合你生活型態的「間歇性斷食」與「低碳飲食」計畫。不需挨餓，也能讓身體切換回燃脂模式。</p>
                    </div>
                    <div class="feature-card">
                        <h3>🏃 Zone 2 科學運動</h3>
                        <p>不需要把自己練到力竭。透過 Zone 2 超慢跑與肌力訓練，以最有效率的方式提升線粒體功能。</p>
                    </div>
                </div>
            </section>

            <section class="section" style="background: var(--bg-cream);">
                <h2 class="section-title">90 天轉變計畫</h2>
                <div class="workflow-step">
                    <div class="step-number">1</div>
                    <div>
                        <h3>第一階段：覺察與歸零</h3>
                        <p>透過數據盤點身體現況，找出發炎源頭。進行為期 2 週的排除飲食 (Elimination Diet)，讓腸道與代謝系統休息。</p>
                    </div>
                </div>
                <div class="workflow-step">
                    <div class="step-number">2</div>
                    <div>
                        <h3>第二階段：重建代謝靈活性</h3>
                        <p>導入 168 斷食與低碳飲食。訓練身體重新學會燃燒脂肪，擺脫對糖分的依賴，解決餐後嗜睡問題。</p>
                    </div>
                </div>
                <div class="workflow-step">
                    <div class="step-number">3</div>
                    <div>
                        <h3>第三階段：原子習慣固化</h3>
                        <p>將飲食與運動融入日常，建立「不費力」的健康自動導航系統。讓健康成為你財富增長的基石。</p>
                    </div>
                </div>
            </section>

             <section class="section" style="text-align: center;">
                <h2 class="section-title">你的健康資產，值得最好的投資</h2>
                <p style="margin-bottom: 2rem; font-size: 1.2rem;">每天 15 分鐘的投入，換來未來 30 年的自由。<br/>立即預約 30 分鐘免費諮詢，讓我們聊聊你的健康目標。</p>
                <div class="cta-group">
                    <a href="/booking" class="cta-btn">填寫預約表單，申請免費諮詢</a>
                    <a href="/quiz" class="cta-btn secondary">還沒準備好？先做代謝年齡測驗 →</a>
                </div>
            </section>
		</main>
		<Footer />
	</body>
</html>
